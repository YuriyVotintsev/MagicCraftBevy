(
    id: "archer_shot",
    activator: OnInputParams((children: [
        SpawnProjectileParams((
            speed: Some(Float(400.0)),
            size: Some(Float(8.0)),
            children: [
                OnCollisionParams((children: [
                    DamageParams((amount: Some(Stat("physical_damage"))))
                ]))
            ]
        ))
    ]))
)
