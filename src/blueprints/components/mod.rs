collect_components!(
    components: [
        projectile,
        straight,
        lifetime,
        pierce,
        growing,
        size,
        collider,
        sprite,
        damage_payload,
        dash,
        orbiting,
        falling,
        find_nearest_enemy,
        on_found,
        on_collision,
        on_area,
        on_expire,
        follow_caster,
        destroy_enemy_projectiles,
        boomerang,
        health,
        dynamic_body,
        move_toward,
        keep_distance,
        use_abilities,
        when_near,
        when_found_target,
        after_time,
        lunge_movement,
        on_death,
        spawn_ability,
        melee_strike,
    ]
);
